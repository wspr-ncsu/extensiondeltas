If change happens on any tab URL, the extension will check if the domain of new URL is matching with the defined and configured domain URL.In case if there is a match all the tabs will be checked if the defined and configured domain URL is already open on any of the tabs and if any tab found the new URL will be opened in the already existing tab and the tab sourcing the URL will be closed.